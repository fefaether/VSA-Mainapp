# 初回セットアップガイド

[🏠 ドキュメントトップ](../index.md) | [⚖️ 利用規約](./terms.md) | [🔒 プライバシーポリシー](./privacy.md)

---


VSAアプリを初めて起動すると、セットアップウィザードが表示されます。このガイドでは、セットアップウィザードの各ステップについて詳しく説明します。

## セットアップウィザードの概要

セットアップウィザードは、VSAを初めて使用するときの基本的な設定をサポートします。

**所要時間**: 2～5分

**ウィザードが表示される場合**:
- VSAを初めて起動したとき
- 設定ファイルが削除されたとき
- 大幅なバージョンアップ時

**スキップ可能**: いつでも「スキップ」ボタンでスキップ可能です

## セットアップのステップ

### ステップ1: ようこそ画面

VSAアプリケーションの紹介ページです。

**表示内容**:
- VSAのロゴとプロダクト名
- 簡単な説明文
- VSAの主な特徴

**説明内容**:
- VRChatの写真管理
- JPEG XL圧縮機能
- メタデータの自動抽出
- アカウント連携機能（クラウド保存、共有リンク等・段階的公開）

**操作**:
- 「次へ」ボタン: 次のステップに進む
- 「スキップ」ボタン: セットアップをスキップしてメイン画面に移動

### ステップ2: 言語とテーマ選択

アプリの表示言語とビジュアルテーマを選択します。

#### 言語設定

**選択肢**:
- **日本語**: すべてのUI要素とドキュメントが日本語で表示
- **英語**: すべてのUI要素とドキュメントが英語で表示

**選択方法**:
1. ラジオボタンまたはドロップダウンから言語を選択
2. 選択が即座に反映される場合があります

**後から変更**:
- 設定 > 外観 > 言語設定から変更可能

#### テーマ設定

**テーマモード**:
- **ライト**: 白基調の配色（明るい環境に推奨）
- **ダーク**: 黒基調の配色（目への負担を軽減）
- **システム連動**: Windowsの設定に自動追従（推奨）

**選択方法**:
1. テーマモードを選択
2. プレビューで見た目が即座に変更される

**後から変更**:
- 設定 > 外観 > テーマ設定から変更可能

#### テーマカラーセレクター

アプリのアクセントカラーを選択します。

**15色のプリセット**:
- 青、スカイブルー、紫、ラベンダー
- ピンク、ホットピンク、赤、オレンジ
- 緑、ライムグリーン、ターコイズ、シアン
- グレー、ゴールド、その他

**選択方法**:
1. 好みの色をクリック
2. アプリの各所の色が変更される

**後から変更**:
- 設定 > 外観 > テーマカラーセレクターから変更可能

### ステップ3: フォルダ設定

VRChatのスクリーンショットフォルダを指定します。

#### スクリーンショットフォルダ設定

VRChatが写真を保存するフォルダを指定します。

**デフォルトパス**:
```
C:\Users\[ユーザー名]\Pictures\VRChat
```

**設定方法**:
1. 「フォルダを選択」ボタンをクリック
2. フォルダ選択ダイアログが開く
3. VRChatの写真フォルダを選択
4. 「フォルダを選択」をクリック

**確認方法**:
- パスが表示される
- 正しいパスか確認

**VRChat標準フォルダ位置**:
```
Windows: C:\Users\[ユーザー名]\Pictures\VRChat
```

#### フォルダが見つからない場合

**ステップ**:
1. Windowsエクスプローラーを開く
2. `C:\Users\[ユーザー名]\Pictures\`を手動で開く
3. `VRChat`フォルダの有無を確認

**VRChatをまだ起動していない場合**:
- VRChatを起動してスクリーンショットを1枚撮影
- `Pictures\VRChat`フォルダが自動作成されます

**カスタムフォルダを使用している場合**:
1. ウィザードで「フォルダを選択」をクリック
2. VRChatで設定しているカスタムフォルダを選択
3. 「フォルダを選択」をクリック

### ステップ4: 自動起動設定

Windows起動時にVSAを自動起動するかを設定します。

**選択肢**:
- **有効**: Windowsを起動するたびにVSAも自動起動
- **無効**: 手動でVSAを起動する必要があります

**有効にするメリット**:
- VRChatプレイ時に常にVSAが起動
- 写真の自動取り込みが可能
- 設定忘れがない

**無効にするメリット**:
- 不要なときはVSAを起動しない
- リソース消費を最小化
- 手動で完全にコントロール可能

**選択方法**:
1. トグルまたはチェックボックスで選択
2. 「有効」にしたい場合はオン、「無効」にしたい場合はオフ

**後から変更**:
- 設定 > 外観 > 開発者モードで確認（開発者向けオプション）
- または、Windowsのスタートアップ設定から変更

### ステップ5: 完了

セットアップが完了し、メイン画面に移動します。

**完了画面**:
- 設定内容の確認
- 「完了」ボタン

**完了後**:
1. 「完了」をクリック
2. メイン画面が表示される
3. ギャラリービューが表示される
4. インポート処理が開始（指定したフォルダがスキャンされます）

## セットアップのスキップ

セットアップを途中でスキップしたい場合：

**方法1: 各ステップで「スキップ」をクリック**
- 表示されているステップをスキップ
- メイン画面に移動
- 残りのステップは表示されない

**方法2: 「スキップ」ボタンをクリック（ウィザード側）**
- すべてのステップがスキップされる
- デフォルト設定でアプリが起動

**スキップ後**:
- すべての設定がデフォルト値に設定される
- 後から設定画面で変更可能

## セットアップの再実行方法

セットアップをやり直したい場合：

### 方法1: 設定ファイルを削除

**ステップ**:
1. VSAを終了
2. Windowsキー + R
3. `%APPDATA%`と入力してEnter
4. `VSA`フォルダを開く
5. `settings.json`ファイルを削除
6. VSAを再起動
7. セットアップウィザードが再表示

### 方法2: ウィザードからの再実行（実装予定）

将来のバージョンでは、設定画面からセットアップを再実行できるオプションが追加予定です。

## トラブルシューティング

### セットアップが完了しない

**症状**: ウィザードが進まない、または応答なくなる

**原因と対策**:

**アプリが応答していない**:
1. Ctrl + Alt + Delete
2. タスクマネージャーを開く
3. VSAプロセスを選択
4. 「タスクの終了」をクリック
5. VSAを再起動

**ディスク容量が不足**:
1. ディスク容量を確認
2. 不要なファイルを削除
3. 数GB以上の空き容量を確保
4. VSAを再起動

**ウィザード中に「次へ」が反応しない**:
1. マウスをクリックし直す
2. キーボードのEnterキーを押す
3. 別のボタンをクリック
4. 反応があるか確認

### フォルダ選択ができない

**症状**: フォルダを選択しようとするとダイアログが表示されない

**原因と対策**:

**管理者権限が不足**:
1. VSAを管理者権限で実行
2. 右クリック > 「管理者として実行」
3. ウィザードを再度実行

**フォルダのアクセス権限**:
1. 対象フォルダを右クリック
2. プロパティを開く
3. セキュリティタブでアクセス権限を確認
4. 読み取り権限があるか確認

### VRChatフォルダが見つからない

**症状**: VRChatをインストールしているのに、フォルダが見当たらない

**確認事項**:

**VRChatの実際のインストール状態**:
1. VRChatが実際にインストールされているか確認
2. VRChatを起動してスクリーンショットを1枚撮影
3. スクリーンショット撮影後、`Pictures\VRChat`フォルダを確認

**フォルダの表示設定**:
1. Windowsエクスプローラーを開く
2. 表示タブから「隠しファイルを表示」をオン
3. 隠しフォルダが表示されるか確認

**カスタムフォルダを使用している場合**:
1. VRChartの設定でカスタム出力フォルダを確認
2. ウィザードでそのフォルダを指定

## ベストプラクティス

### セットアップ時のコツ

**言語設定**:
- インターフェースが理解しやすい言語を選択
- ドキュメントも同じ言語で表示されます

**テーマ選択**:
- 推奨: 「システム連動」でWindowsの設定に合わせる
- 環境や時間に応じて自動的に切り替わります

**フォルダ設定**:
- デフォルトフォルダでない場合は、VRChatの設定を確認
- 複数フォルダを使用している場合は、メインフォルダを指定

**自動起動設定**:
- VRChatプレイ時にはVSAの自動監視を使用したい場合は有効化
- 一時的にVSAを使用しない場合は無効化

## セットアップ後にすべきこと

セットアップが完了したら、以下のステップを進めることをお勧めします：

1. **ギャラリーを確認**: インポートされた写真を確認
2. **メタデータを確認**: 写真詳細でワールド名やユーザー名が表示されているか確認
3. **ゲーム側設定を確認**: [ゲーム側設定ガイド](game-config.md)でVRChat連携を完全にセットアップ
4. **アカウント連携（任意）**: クラウド保存や共有リンクが利用可能な場合は[アカウント管理ガイド](account-guide.md)を参照（段階的ロールアウト）
5. **チュートリアルを実行**: アプリ内のチュートリアルで各機能を学習

## 関連情報

- [基本的な使い方](../basic-usage.md) - VSAの全体的な使い方
- [ゲーム側設定ガイド](game-config.md) - VRChat連携の詳細設定
- [設定画面完全ガイド](settings-guide.md) - 詳細な設定オプション
- [トラブルシューティング詳細ガイド](troubleshooting.md) - 問題が発生した場合
